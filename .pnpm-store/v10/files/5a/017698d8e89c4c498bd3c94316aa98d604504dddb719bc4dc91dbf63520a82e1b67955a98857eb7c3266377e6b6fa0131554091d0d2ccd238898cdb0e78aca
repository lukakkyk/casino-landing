import { shorthands } from "@tamagui/shorthands/v4";
import { themes, tokens } from "@tamagui/themes/v5";
import { fonts } from "./v5-fonts.mjs";
import { media, mediaQueryDefaultActive } from "./v5-media.mjs";
import { shorthands as shorthands2 } from "@tamagui/shorthands/v4";
import { createThemes } from "@tamagui/theme-builder";
import { adjustPalette, adjustPalettes, createV5Theme, defaultChildrenThemes, defaultDarkPalette, defaultLightPalette, hslToString, interpolateColor, opacify, parseHSL, tokens as tokens2 } from "@tamagui/themes/v5";
import { createSystemFont, fonts as fonts2 } from "./v5-fonts.mjs";
import { breakpoints, media as media2, mediaQueryDefaultActive as mediaQueryDefaultActive2 } from "./v5-media.mjs";
const selectionStyles = theme => theme.color5 ? {
    backgroundColor: theme.color5,
    color: theme.color11
  } : null,
  settings = {
    mediaQueryDefaultActive,
    defaultFont: "body",
    fastSchemeChange: !0,
    shouldAddPrefersColorThemes: !0,
    allowedStyleValues: "somewhat-strict-web",
    addThemeClassName: "html",
    onlyAllowShorthands: !0,
    styleCompat: "react-native"
  },
  defaultConfig = {
    media,
    shorthands,
    themes,
    tokens,
    fonts,
    selectionStyles,
    settings
  };
export { adjustPalette, adjustPalettes, breakpoints, createSystemFont, createThemes, createV5Theme, defaultChildrenThemes, defaultConfig, defaultDarkPalette, defaultLightPalette, fonts2 as fonts, hslToString, interpolateColor, media2 as media, mediaQueryDefaultActive2 as mediaQueryDefaultActive, opacify, parseHSL, selectionStyles, settings, shorthands2 as shorthands, tokens2 as tokens };
//# sourceMappingURL=v5-base.mjs.map
