/**
 * Legacy local type — canonical definition lives in @casino/shared-api.
 */
export type PopularGame = {
  id: string;
  title: string;
  subtitle: string;
  playersCount: number;
  imageUrl: string;
};
